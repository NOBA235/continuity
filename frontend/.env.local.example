# Copy to .env.local for local dev (`npm run dev` picks this up automatically).
# NEXT_PUBLIC_API_URL must point at a running instance of the FastAPI backend.
NEXT_PUBLIC_API_URL=http://localhost:8080
